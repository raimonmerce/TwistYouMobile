export type GameSettings = {
  adultMode: boolean;
  colorMode: boolean;
  alcoholMode: boolean;
  extremoMode: boolean;
  masterMode: boolean;
};
export type Page = "landing" | "main" | "game" | "players" | "settings";